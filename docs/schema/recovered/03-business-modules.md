# 加班、補休、請假、公司贈與假、薪資與人事成本

> 本文件只列可由確認節點恢復的內容；所有標示「欄位待恢復」者不得直接產生 migration。

## 加班

重點：依實際班表判定資格；支援跨日；打卡超過申請時段不自動增加認列；一筆加班只能選 `1=加班費` 或 `2=補休`；拒絕後可重申，歷史保留。

| 表名 | 用途 | 狀態 |
|---|---|---|
| `overtime_requests` | 加班申請與時段 | 已確認存在／欄位待恢復 |
| `overtime_approvals` | 審核、拒絕、撤回歷史 | 已確認存在／欄位待恢復 |
| `overtime_compensations` | 最終補償方式 | 已確認存在／欄位待恢復 |

## 補休

重點：來源可為加班、公司贈與、人工調整；核發時保存薪資計價 Snapshot；後續調薪不影響；可部分使用；最早到期優先；待審先凍結；取消原路返還；到期日仍可用，剩餘依原 Snapshot 轉薪；可撤銷重發但不重送加班；結算後不可異動。

| 表名 | 用途 | 狀態 |
|---|---|---|
| `compensatory_leave_credits` | 每批補休額度 | 已確認存在／欄位待恢復 |
| `compensatory_leave_rate_snapshots` | 核發時計價基準快照 | 已確認存在／欄位待恢復 |
| `compensatory_leave_transactions` | 取得、凍結、使用、返還、轉薪與撤銷帳本 | 已確認存在／欄位待恢復 |
| `compensatory_leave_allocations` | 使用與返還對原批次的分配 | 已確認存在／欄位待恢復 |

## 請假

重點：假別、規則、額度、申請、薪資分離；原始額度不可覆寫；實際扣用批次由 allocation 保存；到期／離職後不可用但歷史保留；請假不刪除原班表。

| 表名 | 用途 | 狀態 |
|---|---|---|
| `leave_types` | 假別定義 | 已確認存在／欄位待恢復 |
| `leave_type_rules` | 規則與有效期 | 已確認存在／欄位待恢復 |
| `leave_entitlements` | 員工額度批次 | 已確認存在／欄位待恢復 |
| `leave_balances` | 當前餘額彙總 | 已確認存在／欄位待恢復 |
| `leave_balance_transactions` | 額度異動帳本 | 已確認存在／欄位待恢復 |
| `leave_requests` | 申請主單 | 已確認存在／欄位待恢復 |
| `leave_request_details` | 日期、時段、假別明細 | 已確認存在／欄位待恢復 |
| `leave_request_approvals` | 審核歷史 | 已確認存在／欄位待恢復 |
| `leave_request_allocations` | 明細扣用額度批次 | 已確認存在／欄位待恢復 |
| `leave_request_documents` | 證明附件 | 已確認存在／欄位待恢復 |
| `leave_events` | 結婚、死亡、生產等事件來源 | 已確認存在／欄位待恢復 |

## 公司贈與假

重點：只能由公司贈與；`pay_type_code` 為 `1=有薪`、`2=無薪`；同批不得混用假別、薪資類型、額度或有效期；`granted_minutes > 0`；起訖日必填且核發後不可直接改；逐員工處理，個別失敗可重試；生效前可撤銷，已使用歷史不可抹除。

| 表名 | 用途 | 狀態 |
|---|---|---|
| `company_leave_grant_batches` | 批次共同條件 | 已確認存在／欄位待恢復 |
| `company_leave_grants` | 每位員工的核發結果 | 已確認存在／欄位待恢復 |

## 薪資

重點：員工主檔、薪資設定、薪資項目分離；保留調薪生效歷史；給付性質由薪資項目決定；事件先保存依據、週期事後結算；結算後只能補發、扣回、更正；結算鎖定各法規版本與計算 Snapshot。

| 表名 | 用途 | 狀態 |
|---|---|---|
| `payroll_pending_items` | 尚未進週期但必須後續結算的項目 | 已確認存在／欄位待恢復 |
| `payroll_settlements` | 員工薪資週期結算與鎖定 | 已確認存在／欄位待恢復 |

薪資設定、薪資項目、員工薪資、調薪歷史、薪資單、明細、發薪銀行帳戶與一次性加扣項均已討論，但正式表名與完整欄位待恢復。

## 人事成本

模組已確認包含薪資與雇主負擔的勞健保／就保／職災及勞退等成本；正式表名與逐欄內容目前無法可靠恢復。狀態：**已確認存在／Schema 待恢復**。


